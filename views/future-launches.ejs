<% include ./partials/header %>

<body>
  <% include ./partials/navbar %>

  <div class="top-picture position-relative overflow-hidden p-3 p-md-5 text-center bg-light jumbotron-image-future">
    <div class="col-md-5 p-lg-5 mx-auto my-5">
      <h1 class="display-4 font-weight-normal text-light" style="text-shadow: 2px 2px 20px black;">Future Launches</h1>
      <p class="lead font-weight-normal text-light" style="text-shadow: 2px 2px 20px black;">What are the upcoming launches?</p>
    </div>
    <div class="product-device shadow-sm d-none d-md-block"></div>
    <div class="product-device product-device-2 shadow-sm d-none d-md-block"></div>
  </div>

  <div class="album py-5 bg-light">
    <div class="container">

      <div class="row">
        <% if (launches.length !== 0) { %>
          <% launches.forEach((launch) => { %>
            <% include ./partials/components/card %>
          <% }); %>
        <% } else { %>
            <div class="container">No results found.</div>
        <% } %>
    
      </div>
    </div>
  </div>

<% include ./partials/footer %>